{
  "name": "token-launchpad",
  "version": "0.2.7",
  "author": "DFST",
  "scripts": {
    "test": "jest",
    "local": "jest worker.test --chain=local --cloud=local --compile=false --deploy=true --mint=true --transfer=true",
    "lightnet": "jest worker.test --chain=lightnet --cloud=local --compile=false --deploy=true --mint=true --transfer=true",
    "devnet.local": "jest worker.test --chain=devnet --cloud=local --compile=false --deploy=true --mint=true --transfer=true",
    "devnet.zkcloudworker": "jest worker.test --chain=devnet --cloud=zkcloudworker --compile=false --deploy=true --mint=true --transfer=true",
    "zeko.local": "jest worker.test --chain=zeko --cloud=local --compile=false --deploy=true --mint=true --transfer=true",
    "zeko.zkcloudworker": "jest worker.test --chain=zeko --cloud=zkcloudworker --compile=false --deploy=true --mint=true --transfer=true"
  },
  "devDependencies": {
    "@jest/globals": "^29.7.0",
    "@types/jest": "^29.5.13",
    "@types/node": "^22.7.5",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "typescript": "^5.6.3"
  },
  "packageManager": "yarn@4.4.0",
  "dependencies": {
    "arweave": "^1.15.5",
    "o1js": "^1.8.0",
    "zkcloudworker": "^0.14.9"
  },
  "jest": {
    "preset": "ts-jest",
    "testEnvironment": "node",
    "slowTestThreshold": 1500,
    "testTimeout": 10800000,
    "bail": true,
    "verbose": true,
    "roots": [
      "tests"
    ],
    "setupFilesAfterEnv": [
      "./jest-config.ts"
    ]
  }
}
